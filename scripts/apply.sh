#!/usr/bin/env bash
set -eo pipefail

printf '%s\n' "Patching not yet supported"

# root_dir="$PWD"
# brave_core="$root_dir/brave-browser/src/brave"

# printf '%s\n' "$brave_core"

# # git -C "$brave_core" diff --patch --output "$root_dir/patches/welcome.patch"

# git -C "$brave_core" apply --reject --whitespace=fix "$root_dir/patches/welcome.patch"
